

const devicesSizes = {
    tablet: '900px',
    mobile: '600px',
    mobile2: '400px',
}

export const mediaDevices ={
    tabletMedia: `(max-width: ${devicesSizes.tablet})`,
    mobileMedia: `(max-width: ${devicesSizes.mobile})`,
}
 